// All tweaks to Geode+
// Tweaked by NightEarl015

/*
ServerEvents.highPriorityData(events => {
  const disabledGeodes =[
    'ancient_debris_geode',
    ''
  ]

  disabledGeodes.forEach(disabledGeode => {
    events.addJson(`geode_plus:worldgen/placed_feature/${disabledGeode}`, {
      feature: `geode_plus:${disabledGeode}`,
      placement: [],
    })
  });

})
*/